# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Output of go build
glearn-cli

# Dependency directories (remove the comment below to include it)
vendor/

.env

dist
tags

autoconfig.yaml

!/fixtures/test-block-no-config/autoconfig.yaml
!/fixtures/test-block-auto-config/autoconfig.yaml
!/fixtures/test-block-no-units-dir/autoconfig.yaml

.DS_Store
.vscode
